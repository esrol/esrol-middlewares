{
  "name": "esrol-middlewares",
  "description": "A ES6 Middleware Class for esrol-server-app. Provide a convenient mechanism for filtering web requests entering your application. Register and iterate through the registered middlewares on request. When the last registered middleware is resolved, the passed route as argument is called",
  "version": "0.1.9",
  "main": "index.js",
  "author": "Ivaylo Ivanov <ivaylo.ivanov.ipi@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git://github.com/esrol/esrol-middlewares"
  },
  "bugs": {
    "url": "https://github.com/esrol/esrol-middlewares"
  },
  "license": "MIT",
  "keywords": [
    "http",
    "https",
    "server",
    "middleware"
  ],
  "engines": {
    "node": ">= 4.0.0"
  },
  "config": {},
  "scripts": {
    "test": "node_modules/.bin/mocha ./tests/test",
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha ./tests/test",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "devDependencies": {
    "chai": "^3.4.0",
    "coveralls": "^2.11.4",
    "debug": "^2.2.0",
    "istanbul": "^0.4.1",
    "mocha": "^2.3.3"
  },
  "dependencies": {
    "esrol-errors": "^0.3.4"
  }
}
